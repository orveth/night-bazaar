{
  "name": "@mpp-jams/night-bazaar-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Night Bazaar Phase-0 client: Three.js ghost/body world + charge-01 browser payer (fresh codec per pops skills/payment-credential.md).",
  "scripts": {
    "build": "bun run build.ts",
    "test": "bun test",
    "typecheck": "bun x tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@cashu/cashu-ts": "^4.5.1",
    "@mpp-jams/fetch-with-pop": "file:../../fetch-with-pop",
    "three": "^0.182.0"
  },
  "devDependencies": {
    "@types/three": "^0.182.0",
    "bun-types": "^1.1.0",
    "typescript": "^5.6.0"
  }
}
